{
  "name": "bodybuilding-progress-coach",
  "version": "0.1.0",
  "private": true,
  "description": "Honest, fair tracking of physique and posing progress from photos & video.",
  "license": "Apache-2.0",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "start": "node launch.mjs",
    "dev": "concurrently -n server,client -c yellow,cyan \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm run dev -w server",
    "dev:client": "npm run dev -w client",
    "build": "npm run build -w server && npm run build -w client",
    "seed": "npm run seed -w server",
    "typecheck": "npm run typecheck -w server && npm run typecheck -w client",
    "test": "npm run test -w client"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  },
  "engines": {
    "node": ">=22"
  }
}
